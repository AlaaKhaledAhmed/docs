
# 🏷️ **Semantic Versioning (SemVer) — الإصدار القياسي للفريق**

يُستخدم نظام **Semantic Versioning 2.0.0** لإدارة أرقام الإصدارات في جميع المشاريع.
الصيغة الرسمية:

```
MAJOR.MINOR.PATCH
```

مثال:

```
1.6.4
```

---

# 🧩 **معنى كل رقم**

## 1️⃣ **MAJOR (إصدار رئيسي)**

يرتفع عندما يحدث **تغيير يكسر التوافق** مع الإصدارات السابقة.

### أمثلة (Breaking Changes):

* حذف حقل من JSON
* تغيير اسم حقل
* تغيير نوع بيانات JSON من String إلى Number
* تعديل API endpoint بطريقة تمنع الإصدارات السابقة من العمل
* تغيير هيكل الـ Model بحيث النسخ القديمة تفشل في القراءة

### عند رفع MAJOR:

```
x.y.z → (x+1).0.0
```

---

## 2️⃣ **MINOR (إصدار فرعي — ميزات جديدة)**

يرتفع عند إضافة **ميزة جديدة متوافقة** مع الإصدارات السابقة.

### أمثلة:

* إضافة شاشة جديدة
* إضافة حقل جديد في JSON بدون حذف شيء
* تحسين الـ UI أو الـ UX
* إضافة فلتر أو خيار جديد
* إضافة Endpoint جديد في الـ API غير مؤثر على القديم
* تحسين أداء دون كسر التوافق

### عند رفع MINOR:

```
1.6.4 → 1.7.0
```

---

## 3️⃣ **PATCH (إصدار إصلاحات بسيطة)**

يرتفع عند إصلاح **أخطاء بسيطة** لا تضيف ميزات ولا تغيّر هيكل البيانات.

### أمثلة:

* إصلاح Bug
* تعديل نص
* إصلاح UI لا يؤثر على البيانات
* تحسين صغير في منطق داخلي
* تعديل لا يمس الـ JSON أو API

### عند رفع PATCH:

```
1.6.4 → 1.6.5
```

---

# 🧠 **قاعدة اتخاذ القرار — ماذا نرفع؟**

قاعدة الفريق الذهبية:

> **أصلحت خطأ → PATCH
> أضفت ميزة → MINOR
> كسرت التوافق → MAJOR**

---

# 🧩 **أمثلة JSON — جميع الاحتمالات المحتملة**

## الحالة رقم 1️⃣ — **إضافة حقل جديد (Compatible)**

### JSON قبل:

```json
{
  "name": "Ali"
}
```

### JSON بعد:

```json
{
  "name": "Ali",
  "age": 25
}
```

✔ التطبيق القديم سيعمل بدون مشاكل
✔ تغيير متوافق
→ **MINOR ↑**

**الإصدار:**

```
1.0.0 → 1.1.0
```

---

## الحالة رقم 2️⃣ — **تغيير اسم الحقل (Breaking)**

### JSON قبل:

```json
{
  "price": 20
}
```

### JSON بعد:

```json
{
  "product_price": 20
}
```

❌ التطبيق القديم سيتوقف أو يفشل في التحويل
→ **MAJOR ↑**

**الإصدار:**

```
1.0.0 → 2.0.0
```

---

## الحالة رقم 3️⃣ — **تغيير نوع بيانات الحقل (Breaking)**

### JSON قبل:

```json
{
  "quantity": "4"
}
```

### JSON بعد:

```json
{
  "quantity": 4
}
```

❌ النسخة القديمة تعتمد على String
→ **MAJOR ↑**

**الإصدار:**

```
2.3.1 → 3.0.0
```

---

## الحالة رقم 4️⃣ — **حذف حقل (Breaking)**

### JSON قبل:

```json
{
  "name": "Ali",
  "age": 25
}
```

### JSON بعد:

```json
{
  "name": "Ali"
}
```

❌ النسخ القديمة التي تعتمد على age ستنهار
→ **MAJOR ↑**

---

## الحالة رقم 5️⃣ — **جعل حقل Required بدل Optional (Breaking)**

### قبل:

```json
{
  "name": "Ali"
}
```

### بعد:

```json
{
  "name": "Ali",
  "age": 20   // required
}
```

❌ النسخة القديمة لا ترسل age
→ **MAJOR ↑**

---

## الحالة رقم 6️⃣ — **جعل الحقل Optional بدل Required (Compatible)**

### قبل:

```json
{
  "name": "Ali",
  "age": 20
}
```

### بعد:

```json
{
  "name": "Ali",
  "age": 20   // optional now
}
```

✔ النسخة القديمة تعمل
→ **MINOR ↑**

---

## الحالة رقم 7️⃣ — **تصحيح خطأ في JSON أو Model بدون تغيير الهيكل (Compatible)**

### قبل:

```json
{
  "price": 20,
  "currency": "USD"
}
```

### بعد (Bug fix فقط):

```json
{
  "price": 20,
  "currency": "SAR"
}
```

✔ نفس الحقول
✔ نفس الأنواع
✔ لا يوجد كسر توافق
→ **PATCH ↑**

---

# 📌 خلاصة مقارنة JSON (تلخيص سريع للفريق)

| التغيير          | متوافق؟ | النتيجة |
| ---------------- | ------- | ------- |
| إضافة حقل        | نعم     | MINOR   |
| حذف حقل          | لا      | MAJOR   |
| تغيير اسم حقل    | لا      | MAJOR   |
| تغيير نوع حقل    | لا      | MAJOR   |
| جعل حقل Required | لا      | MAJOR   |
| جعل حقل Optional | نعم     | MINOR   |
| إصلاح قيمة فقط   | نعم     | PATCH   |

---

# 🔄 **آلية المقارنة بين الإصدارات (مهمة لفهم التحديات)**

حسب وثيقة SemVer الرسمية:

* نبدأ مقارنة **MAJOR**
* إذا تساوى → ننتقل لـ **MINOR**
* إذا تساوى → ننتقل لـ **PATCH**
* نقف عند أول اختلاف

مثال:

```
1.6.x > 1.5.x مهما كان PATCH
```

---

# 📝 **سياسة الفريق الرسمية (اختصار مفيد)**

## ✔ متى نرفع MAJOR؟

* أي تغيير يكسر التوافق
* أي تغيير في JSON يمنع النسخة القديمة من العمل
* أي حذف / تغيير اسم / تغيير نوع

## ✔ متى نرفع MINOR؟

* ميزة جديدة
* إضافة JSON جديد دون التعديل على القديم
* تحسينات لا تكسر التوافق

## ✔ متى نرفع PATCH؟

* إصلاح أخطاء
* تحسينات بسيطة
* تغيير قيمة فقط
* نفس الـ structure تمامًا

---

# 🧱 **Template جاهز لكتابة رقم الإصدار قبل الرفع**

```
## Release Type:
- [ ] PATCH
- [ ] MINOR
- [ ] MAJOR

## Reason:
(اشرح نوع التغيير: إضافة ميزة، إصلاح، تعديل JSON…)

## Breaking Changes:
- [ ] Yes
- [ ] No

## Final Version:
x.y.z
```

---

# 🎯 جاهز — وثيقة كاملة للفريق

تقدر ترسلها مباشرة كمعيار رسمي.

إذا تحب، أقدر أحوّلها لك إلى:

* PDF
* صيغة Notion جاهزة
* أو بنية GitHub Wiki

وتقدر تعطيني شعار الفريق علشان أجهزها كـ "Team Standard Document".
