
# 📌 ما هو Semantic Versioning (SemVer)؟

**Semantic Versioning (SemVer)** هو أسلوب قياسي لترقيم إصدارات البرامج بحيث يعبر رقم الإصدار عن **نوع التغيير** الذي حدث في الكود:

الصيغة القياسية:

> `MAJOR.MINOR.PATCH`

مثال:

> `1.6.4`

فكرة SemVer:
من مجرد رقم الإصدار، نقدر نفهم:

* هل التغيير بسيط (Bug fix)؟
* هل أضيفت ميزة جديدة؟
* هل في تغييرات تكسر التوافق مع الإصدارات السابقة؟

---

## 🧩 معنى كل جزء من رقم الإصدار

### 1️⃣ MAJOR (الرقم الأول – إصدار رئيسي)

يرتفع عندما يكون هناك **تغييرات كبيرة تكسر التوافق (Breaking Changes)** مع الإصدارات السابقة.

**أمثلة على تغييرات تستدعي رفع MAJOR:**

* تغيير في هيكل الـ JSON يجعل الإصدارات القديمة لا تستطيع التعامل معه.
* حذف حقل من الـ JSON يعتمد عليه التطبيق القديم.
* تغيير اسم حقل أو نوعه في الـ JSON / Model (مثلاً من `String` إلى `int`).
* تغيير واجهة الـ API بشكل يمنع النسخ القديمة من العمل (تغيير في الـ endpoints أو الـ body بشكل جذري).

**عند رفع MAJOR:**

> `x.y.z  →  (x+1).0.0`
> مثال: `1.6.4 → 2.0.0`

---

### 2️⃣ MINOR (الرقم الثاني – إصدار ميزات جديدة)

يرتفع عند **إضافة ميزات جديدة** أو تغييرات واضحة **لكنها متوافقة** مع الإصدارات السابقة.

**أمثلة على تغييرات تستدعي رفع MINOR:**

* إضافة شاشة جديدة أو Feature جديدة في التطبيق.
* إضافة حقل جديد في الـ JSON مع بقاء الحقول القديمة كما هي.
* تحسين في الـ UI / UX بدون تغيير في شكل البيانات.
* إضافة Endpoint جديد في الـ API بدون تغيير الـ Endpoints القديمة.
* تحسينات في الأداء لا تكسر التوافق.

**عند رفع MINOR:**

> `1.6.4  →  1.7.0`
> (نرفع MINOR ونُصفِّر PATCH)

---

### 3️⃣ PATCH (الرقم الثالث – إصلاحات بسيطة)

يرتفع عند عمل **إصلاحات (Bug Fixes)** بدون إضافة ميزات جديدة وبدون تغيير في هيكل البيانات أو الـ API.

**أمثلة على تغييرات تستدعي رفع PATCH:**

* إصلاح كراش في مكان معيّن.
* تصحيح حسابات / منطق داخلي بدون تغيير في الشكل الخارجي للـ API أو JSON.
* إصلاح مشكلة في واجهة المستخدم لا تؤثر على الـ API أو البيانات.
* تعديل نصوص فقط (Labels, Messages).

**عند رفع PATCH:**

> `1.6.4  →  1.6.5`

---

## 🧠 كيف نقارن بين نسختين؟

حسب مواصفات SemVer الرسمية:

> تتم المقارنة **من اليسار إلى اليمين**:
>
> 1. نقارن الـ MAJOR
> 2. إذا تساوى، نقارن الـ MINOR
> 3. إذا تساوى، نقارن الـ PATCH
>    ونتوقف عند أول اختلاف.

أمثلة:

* `2.0.0` > `1.9.9` (لأن 2 > 1 في MAJOR)
* `1.6.0` > `1.5.9` (لأن 6 > 5 في MINOR)
* `1.6.4` > `1.6.3` (لأن 4 > 3 في PATCH)

---

## 🧩 التغييرات على JSON / Model – جميع الاحتمالات

هذه أهم الحالات التي تواجهنا عادة في الـ Backend / API / Models، وما يقابلها من نوع إصدار:

### ✅ الحالة 1: إضافة حقل جديد في JSON (Compatible)

**قبل:**

```json
{
  "name": "Ali"
}
```

**بعد:**

```json
{
  "name": "Ali",
  "age": 25
}
```

* الإصدارات القديمة تتجاهل `age` أو لا تعتمد عليه.
* التغيير **متوافق**.

👉 **القرار:** زيادة **MINOR**
مثال: `1.0.0 → 1.1.0`

---

### ❌ الحالة 2: حذف حقل من JSON (Breaking)

**قبل:**

```json
{
  "name": "Ali",
  "age": 25
}
```

**بعد:**

```json
{
  "name": "Ali"
}
```

* الإصدارات القديمة ربما تعتمد على `age`.
* التغيير **يكسر التوافق**.

👉 **القرار:** زيادة **MAJOR**
مثال: `1.2.3 → 2.0.0`

---

### ❌ الحالة 3: تغيير اسم حقل (Breaking)

**قبل:**

```json
{
  "price": 20
}
```

**بعد:**

```json
{
  "product_price": 20
}
```

* الكود القديم يبحث عن `price` ولن يجدها.
* التغيير **Breaking**.

👉 **القرار:** زيادة **MAJOR**

---

### ❌ الحالة 4: تغيير نوع حقل (Breaking)

**قبل:**

```json
{
  "quantity": "4"
}
```

**بعد:**

```json
{
  "quantity": 4
}
```

* النسخ القديمة تنتظر String بينما الجديدة ترسل Number.
* سيحدث فشل في القراءة / parsing.

👉 **القرار:** زيادة **MAJOR**

---

### ❌ الحالة 5: جعل حقل Required بعد أن كان Optional (Breaking)

**قبل (age اختياري):**

```json
{
  "name": "Ali"
}
```

**بعد (age أصبح إجباري):**

```json
{
  "name": "Ali",
  "age": 20
}
```

* النسخ القديمة لا ترسل `age`.
* السيرفر الجديد قد يرفض الطلبات القديمة.

👉 **القرار:** زيادة **MAJOR**

---

### ✅ الحالة 6: جعل حقل Optional بعد أن كان Required (Compatible)

**قبل (age مطلوب دائمًا):**

```json
{
  "name": "Ali",
  "age": 20
}
```

**بعد (age أصبح اختياري):**

```json
{
  "name": "Ali",
  "age": 20 // يمكن غيابه
}
```

* الطلبات القديمة ما زالت تعمل.
* الطلبات الجديدة مرنة أكثر.

👉 **القرار:** زيادة **MINOR**

---

### ✅ الحالة 7: تعديل قيمة فقط بدون تغيير structure (Bug Fix)

**قبل:**

```json
{
  "currency": "USD"
}
```

**بعد:**

```json
{
  "currency": "SAR"
}
```

* أي كود يتعامل مع `currency` ما زال يعمل بنفس الشكل.
* التغيير هو تصحيح قيمة فقط.

👉 **القرار:** زيادة **PATCH**

---

## 📌 جدول تلخيصي سريع للفريق

| نوع التغيير في JSON / Model              | متوافق؟ | ما الذي يزيد؟ |
| ---------------------------------------- | ------- | ------------- |
| إضافة حقل جديد                           | نعم     | MINOR         |
| حذف حقل                                  | لا      | MAJOR         |
| تغيير اسم حقل                            | لا      | MAJOR         |
| تغيير نوع الحقل                          | لا      | MAJOR         |
| جعل حقل Required بعد أن كان Optional     | لا      | MAJOR         |
| جعل حقل Optional بعد أن كان Required     | نعم     | MINOR         |
| تعديل قيمة فقط (نفس الحقول ونفس الأنواع) | نعم     | PATCH         |

---

## 🧠 قاعدة اتخاذ القرار (Shortcut للفريق)

قبل أي إصدار جديد، اسأل نفسك هذه الأسئلة بالترتيب:

1️⃣ **هل هناك أي تغيير يكسر التوافق مع الإصدارات السابقة؟**

* نعم → **ارفع MAJOR**
* لا → انتقل للسؤال التالي

2️⃣ **هل أضفنا ميزة جديدة للمستخدم أو للـ API؟**

* نعم → **ارفع MINOR**
* لا → انتقل للسؤال التالي

3️⃣ **هل الذي حدث مجرد إصلاح أخطاء / تحسينات بسيطة؟**

* نعم → **ارفع PATCH**

تمام… سأضيف الآن **فقرة احترافية وجاهزة للنسخ** توضّح كيف نستخدم SemVer في تحديد:

* **التحديث الإجباري (Force Update)**
* **التحديث الاختياري (Optional Update)**
* **عدم وجود تحديث**

وسأرتّبها بشكل واضح جدًا كسياسة رسمية للفريق.

---

# 📌 **سياسة التحديثات في التطبيق بناءً على Semantic Versioning (SemVer)**

هذه السياسة تساعد الفريق على تحديد متى يجب إجبار المستخدم على التحديث، ومتى نكتفي بتحديث اختياري، وذلك بالاعتماد على الفرق بين النسخة المثبتة في جهاز المستخدم **local** والنسخة الموجودة في المتجر أو الـ Backend **remote**.

---

# 🧱 **أولًا: آلية المقارنة (أساسية لفهم التحديث)**

يتم مقارنة الإصدارات من اليسار لليمين:

1. **MAJOR**
2. **MINOR**
3. **PATCH**

ونتوقف عند أول اختلاف.

مثال:
`1.6.x > 1.5.x`
بغض النظر عن رقم الـ PATCH.

---

# 🔥 **ثانيًا: سياسة التحديث المعتمدة للفريق**

## 1️⃣ إذا تغيّر **MAJOR**

> يعني وجود تغييرات **تكسر التوافق**
> النسخ القديمة لن تعمل بشكل صحيح مع النسخة الجديدة من الـ API أو البيانات.

### 👉 القرار:

### **تحديث إجباري (Force Update)**

### مثال:

* local: `1.6.4`
* remote: `2.0.0`

الناتج:
**يجب إجبار المستخدم على التحديث فورًا.**

---

## 2️⃣ إذا تغيّر **MINOR** فقط (مع بقاء MAJOR نفسه)

> يعني إضافة ميزة جديدة
> التغيير متوافق backward compatible
> النسخة القديمة تعمل طبيعي

### 👉 القرار:

### **تحديث اختياري (Optional Update)**

يُنصح المستخدم بوجود ميزات جديدة لكنه غير مُجبر.

### مثال:

* local: `1.5.8`
* remote: `1.6.4`

الناتج:
**تحديث اختياري.**

---

## 3️⃣ إذا تغيّر **PATCH** فقط (مع بقاء MAJOR و MINOR نفسهما)

> يعني وجود إصلاحات عامة
> لا توجد ميزات جديدة
> لا توجد تغييرات تكسر التوافق

### 👉 القرار:

### **لا تحديث إجباري – فقط إشعار بسيط أو تجاهل**

في بعض المشاريع:

* يمكن إظهار إشعار هادئ
* أو حتى تجاهله تمامًا

المهم: **ليس إجباري وليس اختياري مهم**.

### مثال:

* local: `1.6.3`
* remote: `1.6.4`

الناتج:
**لا تحديث ضروري.**

---

# 🚀 **ثالثًا: ملخص مختصر للفريق**

| الاختلاف    | نوع التغيير      | تأثيره على المستخدم            | قرار التحديث       |
| ----------- | ---------------- | ------------------------------ | ------------------ |
| تغيير MAJOR | Breaking Changes | النسخة القديمة لن تعمل         | تحديث إجباري       |
| تغيير MINOR | إضافة ميزات      | النسخة القديمة تعمل بشكل ممتاز | تحديث اختياري      |
| تغيير PATCH | إصلاحات بسيطة    | تغيير غير مهم                  | تجاهل / إشعار بسيط |

---

# 🧩 **المثال الكامل (سيناريو عملي)**

## الحالة: النسخة في المتجر الآن (remote):

```
2.1.3
```

### المستخدم 1

النسخة المثبتة (local):

```
1.9.9
```

الفرق في **MAJOR**
→ تحديث إجباري

---

### المستخدم 2

النسخة المثبتة (local):

```
2.0.4
```

الفرق في **MINOR**
→ تحديث اختياري

---

### المستخدم 3

النسخة المثبتة (local):

```
2.1.1
```

الفرق في **PATCH**
→ لا تحديث مهم

---

# 🎯 القاعدة النهائية (سطر واحد للفريق)

> **MAJOR → تحديث إجباري
> MINOR → تحديث اختياري
> PATCH → تحديث بسيط أو يتجاهل**


